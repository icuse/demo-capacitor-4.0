import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.orange.demo.plugin',
  appName: 'demo-capacitor',
  webDir: 'dist/demo-capacitor',
  bundledWebRuntime: false
};

export default config;
